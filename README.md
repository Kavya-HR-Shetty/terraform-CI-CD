# Terraform EC2 Instance Provisioning

This project demonstrates how to create an **Amazon EC2 instance using Terraform** with best practices such as Infrastructure as Code (IaC), automation readiness, and secure configuration.

---

## 📌 Project Overview

Using Terraform, this project provisions:
- An **EC2 instance** in AWS
- Uses the **default VPC**
- Installs **Apache Web Server** using user data
- Displays a simple HTML page on the browser

This setup is suitable for learning Terraform basics and integrating it with CI/CD pipelines like GitHub Actions.

---

## 🛠️ Prerequisites

Before you begin, ensure you have:

- An **AWS account**
- **Terraform** installed (v1.5+ recommended)
- **AWS CLI** installed and configured
- An **IAM user/role** with EC2 permissions
- Git installed (optional but recommended)

---

## 📂 Project Structure

